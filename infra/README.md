# Infrastructure

Local development and production operational configurations live here:
- `docker-compose.yml` (PostgreSQL 16+, Redis 7+, MinIO)
- Container deployment definitions and environment orchestration

Do not commit secrets. Use environment-specific secret management.

See `docs/deployment.md`, `.claude/skills/devops/SKILL.md`, and `.claude/skills/deployment/SKILL.md`.
