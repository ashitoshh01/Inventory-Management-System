# Shared UI

Reusable, accessible UI primitives and business-agnostic components (Button, Input, Select, Dialog, Card, Table, Badge, EmptyState).

Keep business-specific data fetching and state mutations outside this package.

See `docs/ui.md` and `.claude/skills/frontend/SKILL.md`.
